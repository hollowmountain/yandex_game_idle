# Кадры, снятые дев-сервером через POST /shot. Рабочий материал для промо,
# в сборку игры попадать не должен — платформа считает вес распакованным.
shots/

# Исходный арт для иконки и обложки. Лежит только локально: репозиторий
# публичный, а выкладывать исходники незачем. tools/compose.html ждёт файлы
# именно здесь — без них он не соберёт промоматериалы.
tools/src/
